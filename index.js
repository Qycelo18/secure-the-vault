let code1;
code1 = 3 + 7;

let code2 = 120 / 2 - 20;

let code3 = (3 % 3) + 39;

let message = "The vault has been secured. The combonation is:";

let codeA = code1 + "-" + code2 + "-" + code3;
let codeB = `${code1}-${code2}-${code3}`;

console.log(message, codeA, codeB);
